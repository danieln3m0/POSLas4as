spring.application.name=POSBackend

# Spring DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/gestion_pos?useSSL=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=daniel082004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.generate_statistics=false
spring.jpa.properties.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration Properties
jwt.secret=WriteHereYourSecretStringForTokenSigningCredentials
jwt.expiration=2592000000

# Configuración de Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# Configuración del servidor
server.port=8080

# Deshabilitar Eureka Client
eureka.client.enabled=false
spring.cloud.discovery.enabled=false

# Configuración de logging
logging.level.com.las4as.POSBackend=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE



